spring:
  application:
    name: merchant-business-gateway
  datasource:
    url: jdbc:postgresql://10.250.3.31:5432/Creditor
    username: postgres
    password: 123456
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      #ddl-auto: validate # Options: none, validate, update, create, create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect